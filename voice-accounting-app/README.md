# 语音记账应用

## 功能特点

- 🎤 中文语音输入，自动识别客户、商品、数量和价格
- 📝 手动输入备选方案
- 🔍 智能客户匹配和自动创建
- 💰 自动计算总价
- 📊 发票管理和统计
- 🔒 HTTPS 安全连接

## 快速启动

### 方法1：使用快速启动脚本
```bash
cd /root/jizhang/voice-accounting-app
./start.sh
```

### 方法2：使用管理脚本
```bash
cd /root/jizhang/voice-accounting-app
./manage.sh start
```

### 方法3：使用 systemd 服务
```bash
sudo systemctl start voice-accounting
sudo systemctl enable voice-accounting  # 开机自启动
```

## 访问地址

- 主应用: https://192.168.2.28:3000

## 管理命令

```bash
# 查看应用状态
./manage.sh status

# 查看实时日志
./manage.sh logs

# 停止应用
./manage.sh stop

# 重启应用
./manage.sh restart

# 测试应用响应
./manage.sh test
```

## 保持应用持续运行

### 方法1：使用 nohup（推荐）
```bash
# 启动应用（已在后台运行）
./manage.sh start

# 应用会在后台持续运行，即使关闭终端也不会停止
```



## 语音输入示例

- "张三购买了5个产品，每个20元"
- "李四要了10个零件，每个15元"
- "王五公司维修服务3次，每次100元"

## 客户选择功能

1. 语音输入自动识别客户名称
2. 如果客户不存在，自动创建新客户
3. 在编辑表单中可以修改客户选择
4. 支持从下拉列表选择现有客户
5. 支持输入新客户名称并创建

## 技术栈

- Next.js 16
- React
- TypeScript
- Prisma ORM
- PostgreSQL
- Tailwind CSS

## 项目结构

```
voice-accounting-app/
├── src/
│   ├── app/              # Next.js App Router
│   ├── components/       # React 组件
│   │   ├── CustomerInput.tsx      # 客户选择组件
│   │   ├── EditInvoiceForm.tsx    # 发票编辑表单
│   │   └── ...
│   ├── lib/              # 工具库
│   │   ├── enhancedVoiceParser.ts # 语音解析器
│   │   ├── chineseNumberConverter.ts # 中文数字转换
│   │   └── ...
│   └── ...
├── prisma/               # 数据库模式
├── public/               # 静态资源
├── manage.sh             # 管理脚本
├── start.sh              # 快速启动脚本
└── README.md             # 本文件
```

## 故障排除

### 端口被占用
```bash
# 查找占用端口的进程
lsof -i :3000

# 停止应用
./manage.sh stop

# 或手动杀死进程
pkill -f "node server.js"
```

### 应用无响应
```bash
# 查看日志
./manage.sh logs

# 重启应用
./manage.sh restart

# 测试API
curl -k https://192.168.2.28:3000/api/customers
```

### 语音识别不工作
- 确保使用 HTTPS 访问（浏览器要求）
- 检查麦克风权限
- 确保浏览器支持 Web Speech API

## 开发说明

### 本地开发
```bash
npm run dev        # HTTP 开发服务器
npm run dev:https  # HTTPS 开发服务器（推荐）
npm run build      # 构建生产版本
npm run start      # 启动生产版本
```

### 数据库操作
```bash
npx prisma studio      # 打开数据库管理界面
npx prisma migrate dev  # 运行数据库迁移
npx prisma generate     # 生成 Prisma 客户端
```

